overwrite: true
schema:
  'src/schema/**/*.ts':
    noRequire: true
hooks:
  afterAllFileWrite:
    - prettier --write
generates:
  src/types/generated/schema-types.ts:
    plugins:
      - typescript
  src/types/generated/resolver-types.ts:
    plugins:
      - typescript
      - typescript-resolvers
