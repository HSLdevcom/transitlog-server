overwrite: true
schema: "http://localhost:4000"
documents: null
generates:
  src/types/generated/schema.d.ts:
    plugins:
      - "typescript-common"
      - "typescript-server"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
