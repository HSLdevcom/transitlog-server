overwrite: true
schema: "http://localhost:4000"
documents: null
generates:
  src/types/generated/schema-types.d.ts:
    plugins:
      - "typescript-common"
      - "typescript-server"
  src/types/generated/resolver-types.d.ts:
    plugins:
      - "typescript-common"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
